"use client";

import GlassCard from "@/components/ui/GlassCard";
import PremiumButton from "@/components/ui/PremiumButton";

export default function MatchCard({ match }: { match: any }) {
  return (
    <GlassCard className="flex flex-col gap-[var(--space-md)]">
      {/* Header */}
      <div className="flex justify-between items-center">
        <h3 className="text-xl font-semibold text-[var(--color-text)] tracking-tight leading-tight">
          {match.name}, {match.age}
        </h3>

        <span className="text-sm text-[var(--color-muted)] tabular-nums">
          {new Date(match.createdAt).toLocaleDateString("no-NO")}
        </span>
      </div>

      {/* Distance */}
      <p className="text-sm text-[var(--color-muted)] leading-[var(--line-relaxed)]">
        {match.distance}
      </p>

      {/* Score */}
      <div className="inline-flex items-center gap-2 rounded-full px-4 py-2 text-sm font-medium text-[var(--color-gold)] bg-[var(--color-gold)]/10 border border-[var(--color-gold)]/20 self-start">
        <span className="text-[var(--color-muted)] text-xs font-normal">Matchscore:</span>{" "}
        <span className="text-lg font-semibold">{match.score}%</span>
      </div>

      {/* Breakdown */}
      <div className="flex flex-col gap-[var(--space-xs)] text-[var(--color-text)] text-sm leading-[var(--line-relaxed)]">
        <p>
          <span className="font-medium text-[var(--color-text)]">Hvorfor dere passer:</span>{" "}
          <span className="text-[var(--color-muted)]">{match.summary.whyGood}</span>
        </p>
        <p>
          <span className="font-medium text-[var(--color-text)]">Mulig utfordring:</span>{" "}
          <span className="text-[var(--color-muted)]">{match.summary.challenge}</span>
        </p>
      </div>

      {/* Action */}
      <PremiumButton
        variant="primary"
        className="w-full"
        onClick={() => (window.location.href = `/chat/${match.conversationId}`)}
      >
        Åpne chat →
      </PremiumButton>
    </GlassCard>
  );
}